export XDG_CONFIG_HOME="${HOME}/.config"

# Rust
if [ -e "${HOME}/.cargo/env" ]; then
    . "${HOME}/.cargo/env"
fi

# useful script
if [ -d "${HOME}/.bin" ]; then
    export PATH="${PATH}:${HOME}/.bin"
fi
