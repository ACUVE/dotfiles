#!/usr/bin/env bash

set -euo pipefail

sbx \
    --git \
    --cwd \
    --write "${HOME}/.claude" \
    --write "${CLAUDE_CONFIG_DIR:-${HOME}/.claude}" \
    claude --dangerously-skip-permissions "$@"
